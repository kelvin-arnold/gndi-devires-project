// Component generated with util/create-component.js
import styled from "styled-components";
import classnames from "classnames";

/**
 * Components
 */
export const TextWrapper = styled.span.attrs(() => {
	const className = classnames(["text", true]);
	return {
		className,
	};
})``;
